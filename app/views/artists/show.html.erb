<h2><%= @artist.name %></h2>
<h3>Songs: <%  Artist.songs.each do |song| %>
           <%= link_to song, song_path(song) %>   <%= song.name %></h3>
           <% end %>
<%= link_to 'Edit', edit_artist_path(@artist) %>
