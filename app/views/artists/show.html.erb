<h2><%= @artist.name %></h2>
<h3>Songs: <%  @artist.songs.each do |song| %></h3>
  <br>
           <%= link_to song, song_path(song) %>   <%= song.title %>
           <% end %>
<%= link_to 'Edit', edit_artist_path(@artist) %>
